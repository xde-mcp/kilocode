diff --git a/node_modules/@openrouter/ai-sdk-provider/.bun-tag-7dac70da4266b9c0 b/.bun-tag-7dac70da4266b9c0
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/index.js b/dist/index.js
index f33510a50d11a2cb92a90ea70cc0ac84c89f29b9..9773661acb92678ff0f89e069899d261e136b42e 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1050,7 +1050,8 @@ var OpenRouterProviderMetadataSchema = import_v43.z.object({
 var OpenRouterProviderOptionsSchema = import_v43.z.object({
   openrouter: import_v43.z.object({
     reasoning_details: import_v43.z.array(ReasoningDetailUnionSchema).optional(),
-    annotations: import_v43.z.array(FileAnnotationSchema).optional()
+    annotations: import_v43.z.array(FileAnnotationSchema).optional(),
+    phase: import_v43.z.string().optional()
   }).optional()
 }).optional();
 
@@ -1355,6 +1356,7 @@ function convertToOpenRouterChatMessages(prompt) {
         const parsedProviderOptions = OpenRouterProviderOptionsSchema.safeParse(providerOptions);
         const messageReasoningDetails = parsedProviderOptions.success ? (_g = (_f = parsedProviderOptions.data) == null ? void 0 : _f.openrouter) == null ? void 0 : _g.reasoning_details : void 0;
         const messageAnnotations = parsedProviderOptions.success ? (_i = (_h = parsedProviderOptions.data) == null ? void 0 : _h.openrouter) == null ? void 0 : _i.annotations : void 0;
+        const messagePhase = parsedProviderOptions.success && parsedProviderOptions.data && parsedProviderOptions.data.openrouter ? parsedProviderOptions.data.openrouter.phase : void 0;
         const finalReasoningDetails = messageReasoningDetails && Array.isArray(messageReasoningDetails) && messageReasoningDetails.length > 0 ? messageReasoningDetails : accumulatedReasoningDetails.length > 0 ? accumulatedReasoningDetails : void 0;
         messages.push({
           role: "assistant",
@@ -1363,6 +1365,7 @@ function convertToOpenRouterChatMessages(prompt) {
           reasoning: reasoning || void 0,
           reasoning_details: finalReasoningDetails,
           annotations: messageAnnotations,
+          phase: messagePhase,
           cache_control: getCacheControl(providerOptions)
         });
         break;
@@ -1900,6 +1903,7 @@ var OpenRouterChatLanguageModel = class {
           provider: (_l = response.provider) != null ? _l : "",
           reasoning_details: (_m = choice.message.reasoning_details) != null ? _m : [],
           annotations: fileAnnotations && fileAnnotations.length > 0 ? fileAnnotations : void 0,
+          phase: choice.message.phase || void 0,
           usage: __spreadValues(__spreadValues(__spreadValues({
             promptTokens: (_n = usageInfo.inputTokens) != null ? _n : 0,
             completionTokens: (_o = usageInfo.outputTokens) != null ? _o : 0,
@@ -1971,6 +1975,7 @@ var OpenRouterChatLanguageModel = class {
     let reasoningId;
     let openrouterResponseId;
     let provider;
+    let lastPhase;
     return {
       stream: response.pipeThrough(
         new TransformStream({
@@ -2040,6 +2045,9 @@ var OpenRouterChatLanguageModel = class {
               return;
             }
             const delta = choice.delta;
+            if (delta.phase) {
+              lastPhase = delta.phase;
+            }
             const emitReasoningChunk = (chunkText, providerMetadata) => {
               if (!reasoningStarted) {
                 reasoningId = openrouterResponseId || generateId();
@@ -2110,7 +2118,12 @@ var OpenRouterChatLanguageModel = class {
               if (reasoningStarted && !textStarted) {
                 controller.enqueue({
                   type: "reasoning-end",
-                  id: reasoningId || generateId()
+                  id: reasoningId || generateId(),
+                  providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                    openrouter: {
+                      reasoning_details: accumulatedReasoningDetails
+                    }
+                  } : undefined
                 });
                 reasoningStarted = false;
               }
@@ -2307,7 +2320,12 @@ var OpenRouterChatLanguageModel = class {
             if (reasoningStarted) {
               controller.enqueue({
                 type: "reasoning-end",
-                id: reasoningId || generateId()
+                id: reasoningId || generateId(),
+                providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                  openrouter: {
+                    reasoning_details: accumulatedReasoningDetails
+                  }
+                } : undefined
               });
             }
             if (textStarted) {
@@ -2328,6 +2346,9 @@ var OpenRouterChatLanguageModel = class {
             if (accumulatedFileAnnotations.length > 0) {
               openrouterMetadata.annotations = accumulatedFileAnnotations;
             }
+            if (lastPhase !== void 0) {
+              openrouterMetadata.phase = lastPhase;
+            }
             controller.enqueue({
               type: "finish",
               finishReason,
diff --git a/dist/index.mjs b/dist/index.mjs
index 8a688331b88b4af738ee4ca8062b5f24124d3d81..1a35cc651b978b24d1d167e17f7aea48fcb88c0b 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1015,7 +1015,8 @@ var OpenRouterProviderMetadataSchema = z3.object({
 var OpenRouterProviderOptionsSchema = z3.object({
   openrouter: z3.object({
     reasoning_details: z3.array(ReasoningDetailUnionSchema).optional(),
-    annotations: z3.array(FileAnnotationSchema).optional()
+    annotations: z3.array(FileAnnotationSchema).optional(),
+    phase: z3.string().optional()
   }).optional()
 }).optional();
 
@@ -1320,6 +1321,7 @@ function convertToOpenRouterChatMessages(prompt) {
         const parsedProviderOptions = OpenRouterProviderOptionsSchema.safeParse(providerOptions);
         const messageReasoningDetails = parsedProviderOptions.success ? (_g = (_f = parsedProviderOptions.data) == null ? void 0 : _f.openrouter) == null ? void 0 : _g.reasoning_details : void 0;
         const messageAnnotations = parsedProviderOptions.success ? (_i = (_h = parsedProviderOptions.data) == null ? void 0 : _h.openrouter) == null ? void 0 : _i.annotations : void 0;
+        const messagePhase = parsedProviderOptions.success && parsedProviderOptions.data && parsedProviderOptions.data.openrouter ? parsedProviderOptions.data.openrouter.phase : void 0;
         const finalReasoningDetails = messageReasoningDetails && Array.isArray(messageReasoningDetails) && messageReasoningDetails.length > 0 ? messageReasoningDetails : accumulatedReasoningDetails.length > 0 ? accumulatedReasoningDetails : void 0;
         messages.push({
           role: "assistant",
@@ -1328,6 +1330,7 @@ function convertToOpenRouterChatMessages(prompt) {
           reasoning: reasoning || void 0,
           reasoning_details: finalReasoningDetails,
           annotations: messageAnnotations,
+          phase: messagePhase,
           cache_control: getCacheControl(providerOptions)
         });
         break;
@@ -1865,6 +1868,7 @@ var OpenRouterChatLanguageModel = class {
           provider: (_l = response.provider) != null ? _l : "",
           reasoning_details: (_m = choice.message.reasoning_details) != null ? _m : [],
           annotations: fileAnnotations && fileAnnotations.length > 0 ? fileAnnotations : void 0,
+          phase: choice.message.phase || void 0,
           usage: __spreadValues(__spreadValues(__spreadValues({
             promptTokens: (_n = usageInfo.inputTokens) != null ? _n : 0,
             completionTokens: (_o = usageInfo.outputTokens) != null ? _o : 0,
@@ -1936,6 +1940,7 @@ var OpenRouterChatLanguageModel = class {
     let reasoningId;
     let openrouterResponseId;
     let provider;
+    let lastPhase;
     return {
       stream: response.pipeThrough(
         new TransformStream({
@@ -2005,6 +2010,9 @@ var OpenRouterChatLanguageModel = class {
               return;
             }
             const delta = choice.delta;
+            if (delta.phase) {
+              lastPhase = delta.phase;
+            }
             const emitReasoningChunk = (chunkText, providerMetadata) => {
               if (!reasoningStarted) {
                 reasoningId = openrouterResponseId || generateId();
@@ -2075,7 +2083,12 @@ var OpenRouterChatLanguageModel = class {
               if (reasoningStarted && !textStarted) {
                 controller.enqueue({
                   type: "reasoning-end",
-                  id: reasoningId || generateId()
+                  id: reasoningId || generateId(),
+                  providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                    openrouter: {
+                      reasoning_details: accumulatedReasoningDetails
+                    }
+                  } : undefined
                 });
                 reasoningStarted = false;
               }
@@ -2272,7 +2285,12 @@ var OpenRouterChatLanguageModel = class {
             if (reasoningStarted) {
               controller.enqueue({
                 type: "reasoning-end",
-                id: reasoningId || generateId()
+                id: reasoningId || generateId(),
+                providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                  openrouter: {
+                    reasoning_details: accumulatedReasoningDetails
+                  }
+                } : undefined
               });
             }
             if (textStarted) {
@@ -2293,6 +2311,9 @@ var OpenRouterChatLanguageModel = class {
             if (accumulatedFileAnnotations.length > 0) {
               openrouterMetadata.annotations = accumulatedFileAnnotations;
             }
+            if (lastPhase !== void 0) {
+              openrouterMetadata.phase = lastPhase;
+            }
             controller.enqueue({
               type: "finish",
               finishReason,
diff --git a/dist/internal/index.js b/dist/internal/index.js
index d40fa66125941155ac13a4619503caba24d89f8a..7231178897c8ba9ce8801858295af1136ef3429e 100644
--- a/dist/internal/index.js
+++ b/dist/internal/index.js
@@ -1004,7 +1004,8 @@ var OpenRouterProviderMetadataSchema = import_v43.z.object({
 var OpenRouterProviderOptionsSchema = import_v43.z.object({
   openrouter: import_v43.z.object({
     reasoning_details: import_v43.z.array(ReasoningDetailUnionSchema).optional(),
-    annotations: import_v43.z.array(FileAnnotationSchema).optional()
+    annotations: import_v43.z.array(FileAnnotationSchema).optional(),
+    phase: import_v43.z.string().optional()
   }).optional()
 }).optional();
 
@@ -1309,6 +1310,7 @@ function convertToOpenRouterChatMessages(prompt) {
         const parsedProviderOptions = OpenRouterProviderOptionsSchema.safeParse(providerOptions);
         const messageReasoningDetails = parsedProviderOptions.success ? (_g = (_f = parsedProviderOptions.data) == null ? void 0 : _f.openrouter) == null ? void 0 : _g.reasoning_details : void 0;
         const messageAnnotations = parsedProviderOptions.success ? (_i = (_h = parsedProviderOptions.data) == null ? void 0 : _h.openrouter) == null ? void 0 : _i.annotations : void 0;
+        const messagePhase = parsedProviderOptions.success && parsedProviderOptions.data && parsedProviderOptions.data.openrouter ? parsedProviderOptions.data.openrouter.phase : void 0;
         const finalReasoningDetails = messageReasoningDetails && Array.isArray(messageReasoningDetails) && messageReasoningDetails.length > 0 ? messageReasoningDetails : accumulatedReasoningDetails.length > 0 ? accumulatedReasoningDetails : void 0;
         messages.push({
           role: "assistant",
@@ -1317,6 +1319,7 @@ function convertToOpenRouterChatMessages(prompt) {
           reasoning: reasoning || void 0,
           reasoning_details: finalReasoningDetails,
           annotations: messageAnnotations,
+          phase: messagePhase,
           cache_control: getCacheControl(providerOptions)
         });
         break;
@@ -1854,6 +1857,7 @@ var OpenRouterChatLanguageModel = class {
           provider: (_l = response.provider) != null ? _l : "",
           reasoning_details: (_m = choice.message.reasoning_details) != null ? _m : [],
           annotations: fileAnnotations && fileAnnotations.length > 0 ? fileAnnotations : void 0,
+          phase: choice.message.phase || void 0,
           usage: __spreadValues(__spreadValues(__spreadValues({
             promptTokens: (_n = usageInfo.inputTokens) != null ? _n : 0,
             completionTokens: (_o = usageInfo.outputTokens) != null ? _o : 0,
@@ -1925,6 +1929,7 @@ var OpenRouterChatLanguageModel = class {
     let reasoningId;
     let openrouterResponseId;
     let provider;
+    let lastPhase;
     return {
       stream: response.pipeThrough(
         new TransformStream({
@@ -1994,6 +1999,9 @@ var OpenRouterChatLanguageModel = class {
               return;
             }
             const delta = choice.delta;
+            if (delta.phase) {
+              lastPhase = delta.phase;
+            }
             const emitReasoningChunk = (chunkText, providerMetadata) => {
               if (!reasoningStarted) {
                 reasoningId = openrouterResponseId || generateId();
@@ -2064,7 +2072,12 @@ var OpenRouterChatLanguageModel = class {
               if (reasoningStarted && !textStarted) {
                 controller.enqueue({
                   type: "reasoning-end",
-                  id: reasoningId || generateId()
+                  id: reasoningId || generateId(),
+                  providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                    openrouter: {
+                      reasoning_details: accumulatedReasoningDetails
+                    }
+                  } : undefined
                 });
                 reasoningStarted = false;
               }
@@ -2261,7 +2274,12 @@ var OpenRouterChatLanguageModel = class {
             if (reasoningStarted) {
               controller.enqueue({
                 type: "reasoning-end",
-                id: reasoningId || generateId()
+                id: reasoningId || generateId(),
+                providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                  openrouter: {
+                    reasoning_details: accumulatedReasoningDetails
+                  }
+                } : undefined
               });
             }
             if (textStarted) {
@@ -2282,6 +2300,9 @@ var OpenRouterChatLanguageModel = class {
             if (accumulatedFileAnnotations.length > 0) {
               openrouterMetadata.annotations = accumulatedFileAnnotations;
             }
+            if (lastPhase !== void 0) {
+              openrouterMetadata.phase = lastPhase;
+            }
             controller.enqueue({
               type: "finish",
               finishReason,
diff --git a/dist/internal/index.mjs b/dist/internal/index.mjs
index b0ed9d113549c5c55ea3b1e08abb3db6f92ae5a7..e63e291bf14b1931bfab43b7d1cfb7bf724deece 100644
--- a/dist/internal/index.mjs
+++ b/dist/internal/index.mjs
@@ -970,7 +970,8 @@ var OpenRouterProviderMetadataSchema = z3.object({
 var OpenRouterProviderOptionsSchema = z3.object({
   openrouter: z3.object({
     reasoning_details: z3.array(ReasoningDetailUnionSchema).optional(),
-    annotations: z3.array(FileAnnotationSchema).optional()
+    annotations: z3.array(FileAnnotationSchema).optional(),
+    phase: z3.string().optional()
   }).optional()
 }).optional();
 
@@ -1275,6 +1276,7 @@ function convertToOpenRouterChatMessages(prompt) {
         const parsedProviderOptions = OpenRouterProviderOptionsSchema.safeParse(providerOptions);
         const messageReasoningDetails = parsedProviderOptions.success ? (_g = (_f = parsedProviderOptions.data) == null ? void 0 : _f.openrouter) == null ? void 0 : _g.reasoning_details : void 0;
         const messageAnnotations = parsedProviderOptions.success ? (_i = (_h = parsedProviderOptions.data) == null ? void 0 : _h.openrouter) == null ? void 0 : _i.annotations : void 0;
+        const messagePhase = parsedProviderOptions.success && parsedProviderOptions.data && parsedProviderOptions.data.openrouter ? parsedProviderOptions.data.openrouter.phase : void 0;
         const finalReasoningDetails = messageReasoningDetails && Array.isArray(messageReasoningDetails) && messageReasoningDetails.length > 0 ? messageReasoningDetails : accumulatedReasoningDetails.length > 0 ? accumulatedReasoningDetails : void 0;
         messages.push({
           role: "assistant",
@@ -1283,6 +1285,7 @@ function convertToOpenRouterChatMessages(prompt) {
           reasoning: reasoning || void 0,
           reasoning_details: finalReasoningDetails,
           annotations: messageAnnotations,
+          phase: messagePhase,
           cache_control: getCacheControl(providerOptions)
         });
         break;
@@ -1820,6 +1823,7 @@ var OpenRouterChatLanguageModel = class {
           provider: (_l = response.provider) != null ? _l : "",
           reasoning_details: (_m = choice.message.reasoning_details) != null ? _m : [],
           annotations: fileAnnotations && fileAnnotations.length > 0 ? fileAnnotations : void 0,
+          phase: choice.message.phase || void 0,
           usage: __spreadValues(__spreadValues(__spreadValues({
             promptTokens: (_n = usageInfo.inputTokens) != null ? _n : 0,
             completionTokens: (_o = usageInfo.outputTokens) != null ? _o : 0,
@@ -1891,6 +1895,7 @@ var OpenRouterChatLanguageModel = class {
     let reasoningId;
     let openrouterResponseId;
     let provider;
+    let lastPhase;
     return {
       stream: response.pipeThrough(
         new TransformStream({
@@ -1960,6 +1965,9 @@ var OpenRouterChatLanguageModel = class {
               return;
             }
             const delta = choice.delta;
+            if (delta.phase) {
+              lastPhase = delta.phase;
+            }
             const emitReasoningChunk = (chunkText, providerMetadata) => {
               if (!reasoningStarted) {
                 reasoningId = openrouterResponseId || generateId();
@@ -2030,7 +2038,12 @@ var OpenRouterChatLanguageModel = class {
               if (reasoningStarted && !textStarted) {
                 controller.enqueue({
                   type: "reasoning-end",
-                  id: reasoningId || generateId()
+                  id: reasoningId || generateId(),
+                  providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                    openrouter: {
+                      reasoning_details: accumulatedReasoningDetails
+                    }
+                  } : undefined
                 });
                 reasoningStarted = false;
               }
@@ -2227,7 +2240,12 @@ var OpenRouterChatLanguageModel = class {
             if (reasoningStarted) {
               controller.enqueue({
                 type: "reasoning-end",
-                id: reasoningId || generateId()
+                id: reasoningId || generateId(),
+                providerMetadata: accumulatedReasoningDetails.length > 0 ? {
+                  openrouter: {
+                    reasoning_details: accumulatedReasoningDetails
+                  }
+                } : undefined
               });
             }
             if (textStarted) {
@@ -2248,6 +2266,9 @@ var OpenRouterChatLanguageModel = class {
             if (accumulatedFileAnnotations.length > 0) {
               openrouterMetadata.annotations = accumulatedFileAnnotations;
             }
+            if (lastPhase !== void 0) {
+              openrouterMetadata.phase = lastPhase;
+            }
             controller.enqueue({
               type: "finish",
               finishReason,
